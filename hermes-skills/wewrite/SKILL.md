---
name: wewrite
description: >-
  Use when writing, formatting, generating cover images for, and publishing WeChat Official Account articles to the draft box using the local Hermes adaptation of the Obsidian/OpenClaw WeWrite workflow.
version: 1.0.0
author: Hermes Agent
license: MIT
platforms: [linux]
metadata:
  hermes:
    tags: [wechat, official-account, writing, formatting, draft, wewrite]
    related_skills: [dog-wechat-daily-writer]
---

# WeWrite · 微信公众号内容全流程助手（Hermes 本地适配版）

## Overview

This skill adapts the Obsidian note `20-Library/Skills/Content/wewrite_skill.md` into a local Hermes workflow. It covers the WeChat article pipeline:

```text
热点/选题 → 框架/素材 → 内容增强 → 写作 → HumanizerZH 去 AI 味 → SEO/自检 → 封面/正文配图 → WeWrite 主题排版（默认 fresh-green） → 微信公众号草稿箱
```

It is now the shared cover-image, publisher, and formatter for dog-writing workflows. `dog-wechat-daily-writer` should call this skill for cover generation, article layout, and draft-box publishing instead of using a plain Markdown-to-HTML publisher.

## Local Paths

```text
SKILL_DIR=/root/.hermes/skills/productivity/wewrite
WORK_DIR=/root/.hermes/workspace/wewrite
OUTPUT_DIR=/root/.hermes/workspace/wewrite/output
CONFIG=/root/.hermes/workspace/wewrite/config.yaml
```

## Accounts

Account credentials must come from `/root/.hermes/.env` or process environment. Do not put secrets in this skill document.

Configured accounts:

| Account alias | Account | AppID | Original ID | Secret env var | Notes |
|---|---|---|---|---|---|
| `dog` | 狗狗生活小百科 | `wx27855f8407f2c81c` | `gh_08e4f228bbda` | `DOG_WECHAT_SECRET` | Default safe path for dog workflows; verified via 峰峰's WeChat backend screenshot (上海浦东新区、已实名、login `graysonfeng214@gmail.com`) |
| `default` / `feng` | 峰AI路 | `wx42b46ea46863a720` | unknown | `WECHAT_APP_SECRET_DEFAULT` | Use with `--allow-non-dog-appid` for AI日报 workflows |

The source OpenClaw config had a gotcha: `publish` did not support `--account`, so account switching required editing config. The Hermes adaptation fixes this by requiring explicit account aliases / environment values and refusing unsafe defaults. For non-dog accounts, pass `--allow-non-dog-appid` so the account switch is deliberate.

## Cover Image Generation

This local adaptation now defaults image generation to **ApiYi `gpt-image-2-vip`**. Doubao remains in config as a disabled/manual fallback, but normal workflows should not race providers: ApiYi is the only enabled default provider.

Config path:

```text
/root/.hermes/workspace/wewrite/config.yaml
```

Current provider config:

| Provider | Model | Base URL | Secret env | Status |
|---|---|---|---|---|
| `apiyi` | `gpt-image-2-vip` | `https://api.apiyi.com/v1` | `APIYI_API_KEY` | configured, key currently required; current official choice after Chinese infographic test |
| `doubao` | `doubao-seedream-5-0` | `https://ark.cn-beijing.volces.com/api/v3` | `DOUBAO_API_KEY` | configured but disabled; enable only as manual fallback if 峰峰 explicitly allows |

Use:

```bash
python3 /root/.hermes/skills/productivity/wewrite/scripts/image_gen.py \
  --topic '夏季雨后遛狗护理' \
  --output /root/.hermes/workspace/wewrite/output/cover.jpg
```

Dry-run provider/key check:

```bash
python3 /root/.hermes/skills/productivity/wewrite/scripts/image_gen.py \
  --topic '夏季雨后遛狗护理' \
  --dry-run
```

Hard rule for 峰峰's four WeChat workflows (狗狗写文、狗狗贴图、AI日报写文、AI日报贴图): every cover/background/article illustration must be generated by a text-to-image model and use the user-specified 新海诚系电影感日系动漫画风. Official choice is ApiYi `gpt-image-2-vip` through `scripts/image_gen.py`; do not prefer Hermes `image_generate` for these workflows because it is not the requested ApiYi model. Only use another real T2I provider if `gpt-image-2-vip` fails and the user allows fallback.

Sticker-specific current setting: dog/AI sticker infographics are back to direct text-to-image generation for model testing. Do not use the previous text-free-background + deterministic Chinese overlay/composition flow unless the user asks to restore it.

Style prompt requirements: use `--prompt` when possible, not only `--topic`; include `Makoto Shinkai-inspired cinematic Japanese anime aesthetic`, clear airy skies, gentle clouds, morning/golden-hour natural light, soft volumetric light, subtle backlight/lens flare, rain/glass/water reflections where relevant, detailed city/nature background, clear air, and emotional cinematic composition. For 峰峰's current preference, tune colors to **清新自然、明亮通透、中等偏低饱和但不灰暗**: use light sky blue, fresh green, cream white, and pale warm sunlight; avoid oversaturated colors, heavy color grading, neon glow, high-purity blue/orange contrast, gloomy gray low-saturation output, and over-dark scenes. Keep the output original: do not copy film stills, characters, posters, real logos, or unauthorized people.

Do **not** use `--allow-local-fallback`, pure PIL, HTML/CSS/SVG, Canvas, screenshots, or any local drawing script as the final published image for these workflows. Current sticker testing mode is direct T2I, so do not use deterministic text-overlay composition unless the user asks to restore it. If all text-to-image providers fail, save the text/caption and report the real error, but do not publish a draft with a non-text-to-image placeholder/fallback image.

## Main Script

Use:

```bash
python3 /root/.hermes/skills/productivity/wewrite/scripts/wewrite_publish.py \
  article.md cover.jpg \
  --account dog \
  --author '狗狗生活小百科' \
  --digest '给铲屎官的一份实用提醒'
```

Features:

- Loads `/root/.hermes/.env` automatically.
- Resolves account aliases from local config and environment.
- Generates covers through `scripts/image_gen.py` using ApiYi `gpt-image-2-vip` as the default and only enabled provider.
- Requires a HumanizerZH pass before publishing: remove AI-flavored phrasing, template transitions, over-polished slogans, excessive bold/emoji, vague attribution, and mechanical three-part structures while preserving facts and Markdown/image references.
- Applies WeChat HTML layout with inline CSS, theme-selectable via `--theme`.
- Supports Markdown body images (`![alt](local-path.png)`): uploads local body images through WeChat `media/uploadimg`, replaces them with WeChat-hosted URLs, and embeds them in article content.
- Uploads local cover as permanent thumb material.
- Publishes to WeChat draft API.
- Supports `--dry-run` to render HTML and verify account/token without creating a draft.
- Refuses missing credentials or mismatched dog account appid.

## Theme System（主题系统）

`--theme <name>` switches the render palette. List available themes:

```bash
python3 /root/.hermes/skills/productivity/wewrite/scripts/wewrite_publish.py --list-themes
```

Registered themes (adapted from gzh-design-skill + upstream Obsidian WeWrite v2.0, both MIT):

| Theme name | Label | Best for |
|---|---|---|
| `professional-clean` | 暖棕商务 | Legacy default, general |
| `moyu-green` | 摸鱼绿 | 教程/测评/清单（卡片丰富） |
| `red-white` | 红白色系 | 深度分析/观点（经典编辑风） |
| `graphite-minimal` | 石墨极简 | 科技/设计评论（克制留白） |
| `zen-whitespace` | 留白禅意 | 深度随笔/艺术（衬线大字） |
| `moyu-ticket` | 摸鱼票据 | 测评/工具对比（票据隐喻） |
| `olive-journal` | 橄榄手记 | 内刊/深度复盘（编辑部质感） |
| `tech-blue` | 科技蓝 | 科技/AI 教程（青色点缀） |
| `warm-daily` | 暖粉日常 | 生活/情感类（暖粉主色） |
| `dark-mode` | 深色暗夜 | 极客/夜间阅读（深底蓝字） |
| `elegant-serif` | 优雅衬线 | 文艺/书评（衬线+玫瑰色） |
| `fresh-green` | 清新薄荷（默认） | 健康/自然（薄荷绿） |
| `minimal-gray` | 极简灰 | 商务/专业（低调克制） |
| `vibrant-purple` | 活力紫金 | 创意/品牌（紫金撞色） |
| `magazine-style` | 杂志编辑 | 深度特稿（杂志红） |
| `academic-paper` | 学术论文 | 论文/长文（琥珀+衬线） |
| `rose-romance` | 玫瑰浪漫 | 情感/女性向（玫瑰粉） |

Usage:

```bash
python3 scripts/wewrite_publish.py article.md cover.jpg --account dog --theme moyu-green
```

Each theme maps gzh-design design variables onto this renderer's components (container / title card / h2 / h3 / paragraph / list card / blockquote / inline code / figure shadow). To add a new theme, add a dict entry in `THEMES` in `wewrite_publish.py` with the same keys — no other code changes needed.

Do not use a WeChat-disallowed style block or external CSS; all styling stays inline.

## Changing the Default Theme（切换主题的可复用流程）

Use this procedure whenever the user wants a different **default** render theme (e.g. the 2026-09-05 dog-account switch from `professional-clean` to `fresh-green`). Follow it in order; do not skip verification.

1. **List & pick.** `python3 scripts/wewrite_publish.py --list-themes` → confirm the target theme name exists. Show the user a live preview first (`--dry-run --html-out` + headless-chrome screenshot of the rendered HTML) and get explicit confirmation before changing anything. Default theme must be user-picked, never auto-decided.

2. **Back up everything before touching config.** Copy (a) `scripts/wewrite_publish.py` and (b) `/root/.hermes/cron/jobs.json` to `.bak-<tag>-<timestamp>` files. Set `<tag>` to the change intent (e.g. `freshgreen`).

3. **Change 4-5 sync points** (all must be consistent, or the docs/logs will mislead):
   - **Script default**: `DEFAULT_THEME = "<theme>"` in `wewrite_publish.py` (also update the `--theme` help string "Default: …").
   - **Daily cron prompt**: in `jobs.json`, the写文 task (e.g. `1127064ae418`) prompt's publish command — **explicitly** add `--theme <theme>` (don't rely on the script default; a future default change shouldn't silently alter the cron). Update the in-prompt prose line that says "必须使用 WeWrite … 排版（默认主题…）".
   - **Skilled template**: `dog-wechat-daily-writer/templates/cron_prompt.md` step-13 command + prose (mirror the jobs.json change so regenerated crons stay consistent).
   - **Dog skill doc**: `dog-wechat-daily-writer/SKILL.md` "Publish to WeChat Draft Box" section: heading, command (add `--theme`), prose, and the "Applies the … layout" line.
   - **wewrite own doc**: `wewrite/SKILL.md` theme table — move the `（默认）` marker onto the new default row (only one row should carry `（默认）`).

4. **Verify (non-negotiable):**
   - `python3 -m py_compile` the script.
   - `jobs.json` parses as JSON and the写文 prompt contains `--theme <theme>`.
   - Run a **default** `--dry-run` (NO `--theme` flag) on a real article and grep the rendered HTML for the new theme's signature colors (e.g. fresh-green → `#d1fae5`/`#a7f3d0`/`#f8fdfb`) — proves `DEFAULT_THEME` actually drives output.
   - For a real publish, run without `--dry-run`, then read the draft back via `draft/get` (POST) and grep the returned `news_item[0].content` for the theme signature. Verify `appid`/`draft_media_id` present.

5. **Record it.** Update MEMORY (`memory` tool) with the new default ("狗狗号 wewrite 默认主题已切 <theme>：DEFAULT_THEME=…, cron 显式 --theme, SKILL 已同步"), and note the backup file names for rollback.

6. **Rollback path.** If the user dislikes the change, restore the `.bak-<tag>-*` files for both `wewrite_publish.py` and `jobs.json`, then re-verify per step 4.

### Pitfalls specific to theme switching
- The script default and the cron must BOTH carry the intent — changing `DEFAULT_THEME` alone leaves the cron prompt prose (which names the old theme) stale and misleading.
- The `--help` string hardcodes "Default: …" — it won't auto-follow `DEFAULT_THEME`, so edit it manually or it will lie.
- Only ONE theme row in the `wewrite/SKILL.md` table should carry `（默认）`; grep `professional-clean 默认` / `（默认）` to confirm none is orphaned.
- jobs.json is a dict, not a list — read it with `json.load` then iterate the job array; edit via Python (patch tool refuses this file per prior experience), always rebinding the prompt object before `json.dump`.
- The dog account publishes to **draft box only** (草稿箱), never群发 — a theme switch must not change that.

## Theme Layout Details（各主题渲染要点）

The renderer converts Markdown into WeChat-compatible HTML:

- H1 becomes title, not body content.
- Intro paragraphs get readable body styling.
- H2 sections use a left accent bar and warm clean typography.
- Body-image caption rule for writing workflows: Markdown image alt text is kept only for accessibility/context and should not be rendered as visible `figcaption`. Never publish visible labels such as `正文配图：主人观察...` below article illustrations; if a caption is ever explicitly required, it must be intentional article copy, not a generated/editor prefix.
- Lists become rounded checklist cards.
- Blockquotes become soft quote boxes.
- Paragraphs use `line-height: 1.85`, dark gray text, and mobile-friendly spacing.

## Dog Workflow Integration

For `dog-wechat-daily-writer`, use WeWrite for cover generation and Step 7 publishing:

```bash
python3 /root/.hermes/skills/productivity/wewrite/scripts/image_gen.py \
  --topic '<选题>' \
  --output /root/.hermes/workspace/workflows/dog-wechat-daily-writer/output/cover.jpg
```

Then publish:

```bash
python3 /root/.hermes/skills/productivity/wewrite/scripts/wewrite_publish.py \
  /root/.hermes/workspace/workflows/dog-wechat-daily-writer/output/article.md \
  /root/.hermes/workspace/workflows/dog-wechat-daily-writer/output/cover.jpg \
  --account dog \
  --author '狗狗生活小百科' \
  --digest '<摘要>'
```

Keep the dog skill’s topic-pool logic, topic constraints, and Feishu notification logic, but delegate cover generation, formatting, and draft publishing to WeWrite.

## Verification Checklist

- [ ] `python3 scripts/wewrite_publish.py --help` works.
- [ ] `python3 scripts/image_gen.py --topic '<topic>' --dry-run` shows exactly one enabled provider: ApiYi `gpt-image-2-vip`.
- [ ] `APIYI_API_KEY` is present before expecting real AI image generation.
- [ ] `--dry-run` renders HTML and detects title/digest.
- [ ] WeChat token check succeeds without printing secrets.
- [ ] For real E2E tests, run without `--dry-run`, capture stdout with `tee <publish-json>`, and verify that JSON contains `ok: true`, expected appid, `thumb_media_id`, and `draft_media_id`.
- [ ] For 峰AI路/default account runs, pass both `--account default` and `--allow-non-dog-appid`.
- [ ] The caller records the topic only after successful publish; avoid duplicating `status=` in both a `--status` flag and freeform notes.

Reference: `references/article-e2e-draft-notes.md` contains the condensed E2E article draft verification pattern.

Reference: `references/apiyi-gpt-image-2-vip-notes.md` captures the current ApiYi `gpt-image-2-vip` integration choice, API quirks (`size` may be ineffective, no `n`/`quality`), prompt ratios, and the live Chinese-infographic test result.

Reference: `references/apiyi-default-image-gen-provider.md` captures the full default-backend setup pattern: WeWrite/workflow YAMLs, the Hermes `image_generate` ApiYi plugin, config keys, and verification steps.

Reference: `references/body-image-caption-rule.md` captures the writing-workflow body-image caption pitfall: do not render Markdown alt text as visible captions, and strip generated prefixes such as `正文配图：` from older drafts.

Reference: `references/wechat-image-style-tuning.md` captures the current visual tuning preference for WeChat writing/sticker images: Shinkai-inspired, but fresh-natural, bright, medium-low saturation, and not gray/dark.

## Common Pitfalls

1. **Using the old plain publisher.** For WeChat article drafts, use this WeWrite script so formatting matches the intended workflow.
2. **Duplicating drafts during tests.** Use `--dry-run` unless the user explicitly wants a real draft.
3. **Hardcoding AppSecret in notes.** Store secrets only in `.env`.
4. **Wrong account.** The dog alias resolves to `wx27855f8407f2c81c`; do not rely on defaults.
5. **Complex CSS.** WeChat strips some CSS. Use inline styles and simple tags.
6. **Body image alt text leaks as visible caption.** Old versions of `wewrite_publish.py` rendered Markdown image alt text as a visible `<figcaption>` below the image, showing text like "正文配图：主人观察..." in the draft. The current version strips both the figcaption AND the `正文配图：` prefix from the alt attribute. Future agents should not add any caption-like prefixes to Markdown image alt text in writing workflows.
7. **Vague ending instruction causes unrelated literary text in dog-writer.** The dog-writer skill's "结尾：温暖收束，引导收藏/转发" was too loose — the model would generate dog-themed poetic sentences unrelated to the article topic. The dog-writer skill now has a tightened ending rule (max 1 sentence, topic-only).
8. **Checkmark marker rendered on its own line above list text (WeChat block-level quirk).** The original `flush_ul()` produced `<li><span>✓</span> text</li>`; WeChat's rich-text engine parsed the leading `<li>`/`<span>` as block-level, splitting the ✓ onto its own line above the item text. **Critical lesson (2026-09-06): a local headless-Chrome render showing the marker inline does NOT prove WeChat renders it inline** — WeChat's HTML engine differs from a standard browser. The only authoritative verification is a REAL draft publish read in the WeChat mobile app. Current fixed structure: each list item is a `<p>` row whose checkmark is a **colored inline `<span>✓</span>` followed by a NORMAL space (never `&nbsp;`) then the text** — e.g. `<p ...><span style="color:#10b981;">✓</span> 文字</p>`. Avoid `<ul>`/`<li>`, avoid `&nbsp;`, and when generating any WeChat list, confirm by opening the published draft on a phone; do not rely on local screenshots alone.
9. **Markdown task-list `- [ ] text` rendered as `✓ [ ] text` (checkmark floating outside empty brackets).** The list detector `re.match(r"^[-*]\s+(.+)$")` captured `[ ] 文字` as the item text while `flush_ul` prepended a `✓`, producing `✓ [ ] 文字` with the checkmark outside the brackets. Fixed (2026-09-07): task-list marker `[ ]` / `[x]` / `[X]` is detected and the item is stored as a `task` kind, rendered as `[✓] 文字` (checkmark INSIDE the brackets) using the theme `check_color`; plain `- text` items keep the `✓ 文字` form. The leading literal `[` before the checkmark span also avoids the WeChat leading-`<span>` block-level quirk. When generating a "今日清单/小清单" to-do list, `- [ ]` items now read cleanly as `[✓] 文字` on one line.
10. **`draft/update` requires `articles` as a single OBJECT, not an array.** `wewrite_publish.py --update-media-id <media_id>` updates a draft in place (no duplicate). But `cgi-bin/draft/update`'s `articles` field is a **single article object** (e.g. `{"article_type":"news","title":...,"content":...}`), NOT an array like `draft/add`'s `articles`. Sending `articles: [{...}]` returns `errcode 47001 data format error`. Always POST `draft/update` with `articles` as an object plus top-level `media_id` and `index` (article position, 0-based). Verify the update by reading the draft back via `draft/get` (POST — GET returns 43002) and grepping the content.
